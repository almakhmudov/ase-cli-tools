# pip requirements for ase-cli-tools.
#
# The recommended install path is the conda environment (see environment.yml),
# because PLUMED and its Python interface (py-plumed) are most easily obtained
# from conda-forge. This file covers the pip-installable pieces for users who
# manage ASE and PLUMED separately.
#
# For CPU-only PyTorch, install with:
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu

ase==3.29.0
numpy>=2.4,<3
scipy>=1.17
fairchem-core==2.21.0
torch==2.8.0

# MACE calculators (--calculator mace). Install the MACE suite from source; the
# MACE-POLAR variant also needs graph_electrostatics. See the README.
#   git clone https://github.com/ACEsuit/mace.git && cd mace && pip install .
#   pip install git+https://github.com/WillBaldwin0/graph_electrostatics.git

# Orb calculators (--calculator orb). Weights download by name on first use;
# no checkpoint file is required.
# orb-models

# PLUMED Python interface (needed only for biased runs via --plumed).
# Prefer conda-forge (py-plumed); the pip package requires a working PLUMED
# library/kernel to be present on the system.
# plumed==2.9.2
