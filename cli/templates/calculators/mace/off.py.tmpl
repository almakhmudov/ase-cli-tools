
# --- calculator: MACE-OFF ---------------------------------------------------
from mace.calculators import mace_off

base_calc = mace_off(
    model=CHECKPOINT,
    device=device,
    default_dtype=DTYPE,
)
