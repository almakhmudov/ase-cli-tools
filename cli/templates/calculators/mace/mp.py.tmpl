
# --- calculator: MACE-MP ----------------------------------------------------
from mace.calculators import mace_mp

base_calc = mace_mp(
    model=CHECKPOINT,
    dispersion=DISPERSION,   # D3 dispersion correction
    device=device,
    default_dtype=DTYPE,
)
