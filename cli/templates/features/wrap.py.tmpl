
# --- wrap every frame into the primary cell ----------------------------------
backup_if_exists(WRAPPED)
frames = read(TRAJ, index=":")
for _f in frames:
    _f.wrap()
write(WRAPPED, frames)
print(f"Wrote wrapped trajectory {WRAPPED}.", flush=True)
